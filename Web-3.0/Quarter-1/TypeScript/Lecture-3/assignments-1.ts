// Assignments # 1: Check the parameter in tsconfig.ts which will stop the creation of js file if 
// there is any error is the ts file
// Answer :  noEmitOnError is a parameter in tsconfig.json file that is if enabled and set value to true
// will not emitting files if any errors are reported during compilation
//   "noEmitOnError": true,   /* Disable emitting files if any type checking errors are reported. */
//======================================================================================================


